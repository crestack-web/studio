import React from 'react';
// import { AppProvider } from './contexts/AppContext';
// import { AppProvider } from '../contexts/AppContext';
// TODO: Update the path below if AppContext exists elsewhere
// import { AppProvider } from '../../contexts/AppContext';
// import { AppShell } from './components/layout/AppShell';
// TODO: Fix the import path below if the file exists elsewhere
// import { AppShell } from '../components/layout/AppShell';
import './styles/tokens.css';

// ═══════════════════════════════════════════
//  App — Root component
//
//  Wraps the entire app in the AppProvider
//  which supplies theme, navigation, toast,
//  and user state to all descendants.
//
//  Usage:
//    ReactDOM.createRoot(document.getElementById('root')!).render(
//      <React.StrictMode>
//        <App />
//      </React.StrictMode>
//    );
// ═══════════════════════════════════════════

export default function App() {
  return (
    <div>
      {/* TODO: Replace this with <AppProvider> and <AppShell /> when available */}
      App context provider not found.
    </div>
  );
}

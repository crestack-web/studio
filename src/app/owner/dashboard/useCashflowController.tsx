'use client';

/** Unused — Cashflowpage is self-contained again. Kept only so old imports do not break builds. */
export function useCashflowController() {
  throw new Error('useCashflowController is not used; Cashflowpage is self-contained');
}

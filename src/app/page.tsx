/**
 * Public homepage (/) — same experience as /welcome.
 * Re-export so TrustedBy (company logos) and Presence (countries)
 * stay in one place and neither section is dropped.
 */
export { default } from './welcome/page';
